# 通用 AI 小说项目工作台原型

这是一个带本地服务和 SQLite 持久化的小说项目工作台原型，面向通用小说项目管理，不绑定单本小说。它支持新建小说项目、导入大纲、自动拆章、管理角色出场进度、学习文风、章节完稿评分、Skill 管理、多模型路由和一致性审查。

## 文件

- `index.html`：单页后台原型入口
- `styles.css`：中文 SaaS 后台布局和视觉样式
- `app.js`：样例数据、导入解析、页面渲染和原型交互
- `server.js`：本地 HTTP 服务、状态保存 API、SQLite 记忆库
- `workbench.sqlite`：运行时自动生成的本地数据库，保存项目、章节、文风、模型配置和记忆片段；该文件已加入 `.gitignore`，不要提交 API Key 和个人创作数据

## 使用

启动本地服务：

```bash
node server.js
```

访问：

```text
http://127.0.0.1:4173
```

页面会从 CDN 加载 lucide 图标。直接打开 `index.html` 时可以预览界面，但不会连接 SQLite，刷新持久化请使用 `node server.js`。

## 已覆盖页面

- 项目库：新建项目、导入大纲创建、切换小说项目
- 项目首页：目标章数、覆盖章数、今日任务、健康度、最近评分
- 大纲中心：txt/md 文件导入、分卷结构、章节节点、伏笔追踪
- 章节目录与细纲：由粗纲范围自动拆成逐章目录，并为每章生成可写作细纲
- 角色进度：出场计划、实际出场、缺席风险、关系进展
- 章节写作台：章节列表、正文编辑器、本章粗纲、本章细纲、约 2200 字正文生成、完稿评分
- 文风学习：样章训练、生成约束、高频禁词扫描
- Skill 管理：题材模板、可发表暧昧尺度、去 AI 味、故事路线补强
- 模型配置：GPT、DeepSeek、Gemini、Claude 多供应商和任务路由
- 一致性审查：角色、伏笔、时间线、禁词、尺度和资料风险检查
- 轻量 RAG 记忆增强：SQLite 保存项目、章节、角色、文风和连续性摘要，章节生成前按项目与章节检索相关记忆片段并注入模型提示词
- 去 AI 味审查：禁止用场景物品清单凑字数，优先检查人物欲望、顾虑、选择、后果和物品堆叠问题
- 生产增强：本地向量索引/混合检索、章节事实库、人物状态机、剧情版本、自动回归审查、提示词版本评测、成本与 Token 监控、多轮编辑 Agent

## 本次修复

- 修复大纲文件选择后没有真实读取的问题。
- 修复真实 2000 章总纲导入后“展示 30/2000”误导显示的问题，现在区分覆盖章数和展示节点数。
- 修复章节表误抓角色索引的问题，现在优先解析“2000章完整梗概”下的 Markdown 表格。
- 增加导入失败提示：当前前端原型支持 `.txt`、`.md`、`.markdown`，`doc/docx` 需要后端解析器。
- 增加娱乐圈爽文 Skill：热搜反转、综艺公开场、资源反压、舆论多视角、行业年份锚点。
- 增加可发表暧昧尺度规则：成年人、自愿、留白、写关系后果，不写露骨动作和身体细节。
- 增加娱乐圈高频禁词和章节评分中的禁词/尺度检查。
- 增加主要角色公开背景资料补强和故事路线建议。

## 本次追加

- 将导入的大纲明确拆成两层：粗纲节点和章节目录。
- 粗纲里的范围节点会自动展开为逐章目录，例如 `001-007` 会生成第 1 到第 7 章。
- 每一章自动生成故事化细纲，包含时间、地点、核心、开场、主线推进、分场景字数和章末钩子。
- 章节写作台右侧新增“本章细纲”，正文生成会按细纲创作。
- 章节目标字数统一调整为约 `2200` 字，生成后的草稿会自动补足到 2200 字左右。

## 本次优化

- 修复细纲模板化问题：不再把“粗纲节点 / 写法要求 / 剧情目标”当成正文细纲，改为参考样章格式的故事细纲。
- 针对 `001-007｜刘亦菲` 生成专用前 7 章细纲，第一章包含房租危机、系统激活、预言兑现、刘亦菲拿报纸出现和反噬钩子。
- 修复正文污染问题：正文生成会过滤“细纲对应”“写法上”“粗纲节点”等工作台提示词。
- 第一章正文草稿已补到约 2200 字以上，当前导入项目第一章为 2352 字。
- 新增本地服务和 SQLite 持久化：新建项目、导入大纲、章节细纲、正文、文风、评分、模型配置、Skill 配置都会保存，刷新后恢复。
- 新增数据库记忆表：按项目保存 project、outline、chapter、style、character 记忆片段，2000 章目录会写入章节记忆，后续可用于模型上下文检索。

## 生产增强层

- 向量库/混合检索：当前实现为 SQLite 持久化的本地哈希向量索引，同时叠加关键词、章节距离和来源权重，适合单机原型和中小规模项目；如果要商用集群化，可替换为 pgvector、Milvus、Qdrant 或 Elasticsearch/OpenSearch。
- 章节事实库：从章节标题、细纲、正文摘要、计划角色、伏笔和模型调用记录中抽取结构化事实，供后续连续性审查和检索使用。
- 人物状态机：按角色沉淀最近章节、状态、位置、目标、关系快照和金手指影响，解决每章像独立生成的问题。
- 剧情版本管理：可手动保存当前项目的剧情快照，保留粗纲、细纲、角色、章节摘要和状态，便于回滚对比。
- 自动回归审查：检查向量索引覆盖、事实库覆盖、章节字数区间、计划角色是否落正文、人物状态机缺失等问题。
- 提示词版本评测：对当前正文生成提示词做规则评测，覆盖连续性、人物状态、字数、去 AI 味和审查约束。
- 成本与 Token 监控：真实模型调用后记录 provider、model、任务、输入/输出 Token 和估算成本。
- 多轮编辑 Agent：为当前章节生成诊断、结构修复、人物鲜活、去 AI 味和验收五轮修订方案。
